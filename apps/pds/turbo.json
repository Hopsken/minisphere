{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "deploy": {
      "dependsOn": ["build", "db:migrate:remote"]
    },
    "dev": {
      "with": ["@minisphere/directory#dev"]
    }
  }
}
